# Streamlit Cloud 部署步驟

## 步驟一：安裝 Git
下載：https://git-scm.com/download/win
一路按 Next 安裝，完成後重開 PowerShell。

## 步驟二：建立 GitHub 帳號
去 https://github.com 註冊（免費）。

## 步驟三：推送程式碼到 GitHub

```powershell
cd "C:\Users\mark\.gemini\antigravity\scratch\streamlit_travel_planner"
git init
git config user.email "你的email@gmail.com"
git config user.name "你的名字"
git add .
git commit -m "智慧旅遊規劃師 初始版本"
```

然後在 GitHub 建立新 repo，名稱填 travel-planner，再執行：

```powershell
git remote add origin https://github.com/你的帳號/travel-planner.git
git branch -M main
git push -u origin main
```

## 步驟四：部署到 Streamlit Cloud
1. 去 https://share.streamlit.io 用 GitHub 登入
2. 按 New app
3. 選 travel-planner repo
4. Main file: app.py
5. 按 Deploy

約 1-2 分鐘後取得公開網址，手機直接開啟！

## 注意：資料保存問題
Streamlit Cloud 免費版重啟後資料會清空。
建議旅遊前在本地輸入好資料，或之後升級接 Supabase 資料庫。
